# /etc/cron.d/artemis-brolog-cron
#
# crontab to maintain Bro file logs

SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/sbin:/bin:/usr/sbin:/usr/bin

00  *  *  *  * root /usr/bin/artemis-brolog-cleanup > /dev/null 2>&1
